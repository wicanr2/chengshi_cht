# 拿掉 DATA/SOUNDDAT.PSF，用**基本圖形集**開機。
#
# 問的是：`sounddat.psf` 到底從哪個目錄讀？根目錄也有一份（2012 重打包
# 換過第 2 段），先前只能靠「檔頭存裸檔名 ＋ 姊妹欄位實測是 C:\DATA\」推。
# 訊息檔那一欄拿掉檔案會讓遊戲印出它自己組的完整路徑，音效這一欄照做一次
# 就知道。
#
# 三種可能的結果都分得出來：
#   印 `Cannot find ...C:\DATA\sounddat.psf` → 只讀 DATA\，根目錄那份沒人用
#   照常開起來                              → 有退路，可能讀了根目錄那份
#   印別的路徑                              → 規則跟訊息檔不一樣
wait 6
shot 00-boot
wait 4
shot 01-later
