# 模擬城市（SimCity, 1989, Maxis）繁體中文重製 — chengshi_cht

把 Will Wright 的《模擬城市》完整重製到 Go / Ebiten，再做繁體中文化。
定位是**文化資產保存**：留下一份華人玩家能玩完、能讀懂、也能考證的版本，
連同 1990 年代軟體世界代理的珍藏版 29 中文說明書一起保存。

方法論沿用《地下城主》(`~/cht/dungeon_master/`)、《臥龍傳》(`~/cht/dragon/`) 那一套，
但這個專案有兩個決定性的差別：

> **一、模擬引擎的原始碼可讀，所以規則層不做反組譯。**
> Electronic Arts 在 2008 年以 GPL-3.0 釋出的 **Micropolis**（Don Hopkins 從
> DUX Software 的 X11 版整理出來的 SimCity 原始碼）就是本專案規則層的一手證據。
>
> **二、原版當年沒有中文版。**
> 軟體世界代理的是英文版遊戲 ＋ 中文說明書。所以繁中化不是「還原某個中文版」，
> 而是**第一次把這款遊戲翻成繁體中文**，而譯名的一手史料就是那本說明書。

---

## 0. 最高紀律：規格未確認，不寫任何一行引擎程式

任何機制（分區成長、電力傳導、交通生成、人口、地價、汙染、犯罪、稅收與預算、
災難、城市評分、存檔格式、圖形解碼、訊息觸發、隨機數）在 Micropolis 原始碼裡讀出來
並寫成筆記之前，**不得憑手冊、攻略、社群 wiki、其他重製專案或直覺實作**。

「有原始碼」不等於「可以照抄」，也不等於「不必讀就知道」。原始碼降低的是**取得證據的成本**，
不是**確認證據的義務**。

### 四道閘門，逐道通過

| 閘門 | 判準 | 產出 |
|---|---|---|
| **G1 — 機制確認** | 該機制的程式碼在 Micropolis 裡定位到、讀完、寫成筆記，含**原始檔 SHA-256 ＋ `檔名:行號` ＋ 原始函式名**（如 `s_scan.c:412 PopDenScan`） | `docs/re/NN-*.md` |
| **G2 — 規格收攏** | 筆記收攏成可實作的規格，每條敘述標推論等級，未解處明寫「未解」不留空白 | `docs/spec/*.md` 標 **READY** |
| **G3 — 實作** | 只有 READY 的規格可以動手；實作完成要對原版行為驗收，不是只跑單元測試 | Go 程式碼 ＋ 驗收證據 |
| **G4 — 接線** | 那份筆記在 `docs/re/00-wiring-status.md` 登記，狀態與程式碼一致 | 接線表一列 ＋ `TestWiringStatus` 綠 |

**G4 不是形式。** 前三道各自都會綠，而結論仍然可以躺在筆記裡沒人用——荒野遊俠踩過：
公式早就解出來寫成筆記了，remake 三處還傳著寫死的常數，而**編得過、測得過、玩得動**。所以：

- **每寫完一份 `docs/re/NN`，同一輪就要在接線表登記**（已接／未接／不適用）。
- **每解出一個係數或表格，回頭 grep 誰在等它。** 尤其看呼叫端：
  **參數列裡「所有呼叫端都傳同一個字面常數」的位置，就是還沒接上的訊號。**
- 引用寫在**用到那個結論的地方**，不是集中在檔頭。

- **`docs/spec/` 沒有 READY 標記 = 不准開 `internal/sim/`。** 不因為「先寫個雛形」放寬。
- 卡住不是動工的理由。撞牆時換方法（讀呼叫端 → 讀 `sim.h` 的陣列宣告 → 讀 X11 版的 Tcl
  介面腳本 → DOSBox 實跑對拍 → 反組譯 `SIMCITY.EXE`），在筆記記下「卡在哪、試過什麼、
  下一個入口是哪支函式」，不要寫「暫緩」或「先照手冊做」。
- 例外只有一種：**Micropolis 真的沒涵蓋**（DOS 版專屬的圖形集、劇本檔、訊息檔、音效格式），
  這時要在規格裡明寫「依 X 暫代，原始碼未涵蓋」，並列入待補清單；
  不得在正文寫成已確認。

### 推論等級（每條斷言都要帶）

`已確認`（讀到原始碼／讀到資料檔 bytes 並解釋得通）／`強證據`（多處互相印證但未直讀）／
`假說`（合理但只有單一來源）／`未解`。

**新結論與既有已確認常數衝突時，先假設新結論是錯的。** 降級比升級容易犯錯。

---

## 1. 證據來源與 oracle 優先序

### 1.1 優先序（衝突時高的贏）

| 序 | 來源 | 適用範圍 | 位置 |
|---|---|---|---|
| 1 | **Micropolis C 原始碼** | 模擬規則層的一切行為 | `workplace/ref/micropolis/`（gitignore）|
| 2 | **DOS 1.10 資料檔本身** | 圖形集、劇本、訊息、音效、城市存檔的格式 | `workplace/dos110/` |
| 3 | **DUX X11 版的 Tcl 腳本與 XPM** | 介面語意、字串、選單結構、`.cty` 城市檔 | `workplace/x11/` |
| 4 | **DOS 執行檔反組譯** | DOS 版專屬行為（Micropolis 不涵蓋）| `workplace/ida/`，見 §2.4 |
| 4 | **DOSBox 實跑** | 「讀出來的行為」需要眼睛確認時 | 見 §1.5 |
| 5 | **軟體世界珍藏版 29 中文說明書** | 譯名與當年台灣用語的一手史料 | `珍029-模擬城市.rar` |
| 6 | 官方英文手冊（IBM PC 版）| 規則輔助 | `workplace/research/simcity-ibm-manual-ocr.txt`；原件 <https://archive.org/details/simcity_ibm_manual> |
| 7 | 社群攻略、維基、論壇、雜誌回顧 | 最後才用，且要標明 | 網路 |

低優先來源只能在高優先來源缺席時暫代，且要標明。
**一手資料贏二手推論**：檔案內容 > 檔名、目錄結構、命名慣例、其他人的文章。

⚠ **這條在本專案特別容易犯**：模擬城市累積了三十幾年的「玩家共識」，
量大、講得很篤定，而且**經常是錯的或跨代混用的**。查證時已經抓到三個現成的例子：

| 玩家共識 | 一手證據說什麼 |
|---|---|
| 「7% 是黃金稅率」 | IBM 手冊寫的是 `The optimum tax rate for fast growth is between 5 and 7%`——是**區間**，7% 只是上緣；手冊也沒說預設值是 7% |
| 「Dullsville 是最難的劇本」（SimCity Fandom）| 手冊標的是 **Easy**；Very Difficult 的是 San Francisco 1906、Hamburg 1944、Boston 2010 |
| 「水管沒接所以不長」 | **一代沒有自來水系統**。全份手冊的 water 只指地形水域，自來水是 SimCity 2000 以後的機制 |

三個例子的共同形狀：**跨代混用**、**把建議區間講成單一數字**、**把玩家意見講成官方設定**。
社群共識與原始碼衝突時原始碼贏，而且要把衝突記進筆記——衝突通常代表共識描述的是
另一代、某個特定版本，或某個 bug。

### 1.2 Micropolis 是什麼、能信到什麼程度

2008 年 Electronic Arts 為了 OLPC 專案，把 SimCity 的原始碼以 **GPL-3.0** 釋出，
去掉商標後命名為 **Micropolis**，由 Don Hopkins 整理。它的血緣是：

```
Will Wright 的原版模擬引擎（C）
  └─ DUX Software 1993 的 X11 移植（Don Hopkins 做介面，Tcl/Tk）  ← 本地有執行檔封存
       └─ 2008 Micropolis（GPL-3.0 釋出）                          ← 規則層一手證據
```

**它不是 DOS 版的原始碼。** 模擬引擎（`s_*.c`）是 Will Wright 那一份的直系後代，
可信度高；但介面層是 X11／Tcl 的，**與 DOS 版無關**。所以本專案的分工是：

- **規則層讀 Micropolis** — 每一格怎麼長、電怎麼傳、車怎麼塞、錢怎麼算。
- **呈現層讀 DOS 1.10 資料檔** — 畫面長什麼樣、訊息寫什麼字、劇本怎麼設定。

引用時一律寫 `檔名:行號` 加原始符號名，例如：

```
Micropolis — src/sim/s_scan.c:412 PopDenScan()
```

**不得把 Micropolis 的符號名改寫成自己的推測語意再引用。** 原始符號名是可稽核身分，
語意用附加說明表達，不取代原始名稱。

### 1.3 Micropolis 原始碼地圖（入口速查）

⚠ **下表整份的推論等級是「假說（憑印象寫的，尚未對照封存）」。**
取得 Micropolis 封存後的第一輪工作就是逐列核對，核對過的改標「已確認」並補上
檔案大小與 SHA-256；核對不到的整列刪掉。**在核對之前不得引用本表當證據。**

| 檔案（假說） | 推測內容 |
|---|---|
| `src/sim/sim.h`、`macros.h` | 全域地圖陣列、tile 常數、結構宣告。**動任何欄位前先讀這裡** |
| `src/sim/s_sim.c` | 模擬主迴圈、每一步做什麼、階段切分 |
| `src/sim/s_scan.c` | 人口密度、汙染、地價、犯罪的掃描 |
| `src/sim/s_power.c` | 電力網傳導 |
| `src/sim/s_traf.c` | 交通生成與壅塞 |
| `src/sim/s_zone.c` | 分區格的成長與衰退 |
| `src/sim/s_eval.c` | 城市評分、市長民調 |
| `src/sim/s_disast.c` | 災難（火災、水災、空難、龍捲風、地震、怪獸）|
| `src/sim/s_gen.c` | 地形產生 |
| `src/sim/s_fileio.c` | 城市存檔讀寫 |
| `src/sim/s_msg.c` | 訊息觸發條件 |
| `src/sim/w_*.c`、`g_*.c` | X11／Tcl 介面與繪圖層。**與 DOS 版無關，只當語意參考** |

### 1.4 DUX X11 版的用法（介面語意與資料的第二來源）

`Rare simcity.zip` 是 DUX Software 1993 年的 **Multi Player SimCity for X11** 發行包
（SGI Irix／SunOS／Solaris 三份），裡面**沒有 C 原始碼**，但有：

- **30 個 `.tcl` 介面腳本**（純文字）：選單、對話框、按鈕標籤、預算面板、
  查詢視窗的欄位名稱。這是「原版介面到底怎麼稱呼一件事」的可讀來源。
- **154 個 `.xpm` 美術**（純文字）：圖示、圖表、demand 指標。
- **46 個 `.au` 音效**。
- **23 個 `.cty` 城市檔**：`.cty` 是 SimCity 跨平台通用的城市存檔格式，
  可以拿來當格式解析的樣本。
- `SimCity` 執行檔本身（各平台原生二進位）。

**它不是獨立證據**：Micropolis 就是從這一支整理出來的，所以「換個封存再看一次同一段
邏輯」不能冒充第二份佐證。它的價值在**介面字串**與 **`.cty` 樣本**。

⚠ 這份是**未授權的商業發行包**（README 裡還在賣 $49 的授權）。
不入版控、不進發行包、不引用它的二進位內容。

### 1.5 實跑對拍

- **DOS 版用 DOSBox**（docker 化，可送鍵、可截圖）。`cycles` 要寫死不要用 `auto`——
  可重現性的敵人。設定先查 `~/.claude/knowledge-base/retro/dosbox-game-configs.md`。
- **逐 tick 對拍是本專案最有力的驗收工具**：模擬城市的狀態是一張
  128×128 的格子陣列加上幾十個純量，全部可以序列化。把同一顆隨機種子、同一串操作
  餵進 Micropolis 與 Go 版，逐 tick 比對整張地圖與所有純量——**這件事在地下城主
  做不到（跨引擎輸入格式對不上），在這裡做得到**，因為兩邊的狀態就是同一張陣列。
  這是本專案相對於前幾個專案最大的機會，優先把它建起來（`rulebook/60`）。
- 實跑是**驗證工具，不是證據來源**：畫面對得起來只證明「這一條路徑對」，
  不能取代讀原始碼。

---

## 2. 原版素材盤點

### 2.1 DOS 版 1.10（`SimCity_DOS_EN_v110.zip`，玩家自備，不入版控）

解開為 `workplace/dos110/`，69 個檔案：

| 檔案／群組 | 目前認知 | 等級 |
|---|---|---|
| `SIMCITY.EXE` (126 KB) | 主程式 | 已確認（唯一的主執行檔）|
| `SETTINGS.EXE` (36 KB) | 顯示模式設定，改寫 `SIMCITY.CFG` | 強證據（`read.me` 明載）|
| `SIMCITY.CFG` (548 B) | 純文字設定檔，自帶解碼表：八種螢幕模式（`H` Hercules／`C` CGA Mono／`T` Tandy Color／`M` Hires EGA Mono／`e` Lores EGA Color／`E` Hires EGA Color／`V` Mono VGA/MCGA／`2` 256 Color VGA/MCGA）、搖桿（IBM／Covox）、音效、Covox 聲道、`Graphics Set` | **已確認**（檔案本身就是明文說明）|
| `CEGA/`、`sega/`、`mcga/`、`MONO/` | 四套顯示模式的圖形資料。**檔名規則是 `<圖形集><模式>`**，例如 `WESTCEGA.PGF` ＝ Wild West 集的 Hires EGA Color 版 | 已確認（`SIMCITY.CFG` 的 `Graphics Set: WESTCEGA` 直接對上）|
| 模式目錄對應 | `CEGA` ＝ Hires EGA Color；`sega` ＝ Lores EGA Color；`mcga` ＝ 256 色 VGA/MCGA；`MONO` ＝ 單色 | 假說（由 CFG 的模式清單 ＋ 各目錄檔案大小分群推得，`CEGA` 那一列另有 CFG 直接佐證）|
| `*.PGF` | 圖形資料，每個模式一組 | 假說（副檔名推測）|
| `*.PPF` | `*ntro`（開場）與 `*scen`（劇本畫面）的圖片 | 假說 |
| `SCENARIO/*.PSN`（8 個）| 劇本：`BERN`、`BOSTON`、`DETROIT`、`DULLSVIL`、`HAMBURG`、`RIO`、`SANFRAN`、`TOKYO` | 已確認（檔名與手冊的八個悲情城市對得上）|
| `DATA/*_MSG.PTF` | 各圖形集的訊息文字。**中文化的主要目標之一** | 假說 |
| `DATA/*_SND.PSF`、`SOUNDDAT.PSF`、`SOUNDDAT.V4` | 音效資料 | 假說 |
| 資料集前綴 `ASIA`／`MEDI`／`WEST`／`FUSA`／`FEUR`／`MOON` | 六組資料片圖形集：對應手冊與雜誌講的「古城風情系列」與「回到未來系列」 | 強證據（前綴語意 ＋ 骨灰集散地頁面列出兩套資料片）|

⚠ **這份是破解版，不是乾淨的原版。** `read.me` 由 "Knight Rider" 署名，明載
「This version has had all the copy protection removed」，並附了作弊程式
`SIMCHEAT.EXE`。後果有三：

1. `SIMCITY.EXE` 已被改過，**反組譯出來的不是原廠碼**，任何從它得出的結論都要標明。
2. 部分檔案的時間戳是 1996 與 2012，與 1991-05 的原廠檔案混在一起——
   **哪些被動過還沒查**，這是第一輪要做的事（列檔案時間戳分群）。
3. 要拿它當 oracle 之前，先另尋一份未破解的 1.10 副本比對。**在那之前，
   從這份得到的行為結論一律標「假說」。**

### 2.2 X11 版（`Rare simcity.zip`，玩家自備，不入版控）

見 §1.4。三個 tarball（SGI 2.11／SunOS 2.11／Solaris 2.14）內容大致相同，
差別只在原生執行檔。

### 2.3 軟體世界珍藏版 29 中文說明書（`珍029-模擬城市.rar`）

骨灰集散地「軟體世界說明書補完計劃」的掃描：**56 張 JPG**（跨頁掃描，約 2200×1100），
封面可見「軟體世界／珍藏版 29／16 BIT／2 片裝／NT 180 元」與 SimCity 標題。
掃描計劃的說明檔要求「請勿在掃描檔加上其他符號或用來牟利」——**掃描件本身不入版控、
不進發行包**，只有轉錄出來的文字進 `docs/manual-cht/`。

已從第 13–14 頁確認的譯名樣本（等級：已確認，出自掃描件本文）：

| 原文 | 軟體世界譯名 |
|---|---|
| SCENARIOS | 悲情城市 |
| DULLSVILLE | 達斯維利 |
| SAN FRANCISCO | 舊金山 |
| TOKYO ／ DETROIT ／ BOSTON | 東京／底特律／波士頓 |
| RIO DE JANEIRO | 里約熱內盧 |
| HAMBURG ／ BERN | 漢堡／伯恩 |
| （通關目標城市）| 明日之都 |
| （通關獎勵）| 市鑰 |

### 2.4 兩層策略（使用者定案 2026-08-29）

**規則層讀 Micropolis，呈現層做 DOS 1.10。** 反組譯是**退路不是主線**：
只有當某個行為在 Micropolis 找不到、又不能由 DOS 資料檔自證時才動 IDA，
而且要先處理 §2.1 的破解版問題。順序永遠是**先資料後反組譯**——
能由資料自證的先自證，因為那不必信任反組譯工具鏈。

---

## 3. 文件與中文化政策

### 3.1 說明書要整理成完整 markdown

| 來源 | 產出 | 要求 |
|---|---|---|
| 軟體世界中文說明書（56 張掃描）| `docs/manual-cht/` | **逐頁完整轉錄**，保留 1990 年代台灣代理商的用語、譯名、行文與編排脈絡 |
| 官方英文手冊（IBM PC 版，OCR 全文已取得）| `docs/manual/` | 轉 markdown ＋ 繁中翻譯，當規則的輔助 oracle。**OCR 有錯字，引用前要對原掃描件核字** |
| 社群攻略、雜誌回顧 | `docs/walkthrough/` | 繁中化，標明來源與版本 |

**中文說明書那份是保存重點。** 它是當年台灣遊戲文化的一手史料：把「悲情城市」
「明日之都」「市鑰」這些選字、當年的行文體例都留下來，不要用現代用語「潤飾」掉。
與原文不一致之處照原樣轉錄，另加註記說明差異，不要直接改成正確答案。
圖說、表格、示意圖都要交代，掃描件裡讀不清的字標 `〔字跡不清〕` 而不是猜一個填上。

### 3.2 譯名與文字工程

- 統一譯名表 `translations/glossary.md` 是唯一真相，**軟體世界說明書的既有譯名優先採用**
  （這是保存的一部分），與原文衝突時記在表上說明取捨。
- **說明書沒涵蓋的詞才自己譯**，而且要在表上標「說明書未收，本專案新譯」。
  說明書是一本 30 頁上下的操作手冊，遊戲內字串一定比它多。
- 所有玩家看得到的字串都放語言資料檔，Go 只留穩定的 key。文字有三個來源，
  三邊都要進翻譯目錄：
  - **DOS 資料檔內**：`DATA/*_MSG.PTF` 的訊息文字、劇本說明。
  - **DOS 執行檔內硬編碼**：選單、按鈕、數字格式。
  - **X11 版 Tcl 腳本**：可讀的介面字串，當對照用（不是移植目標）。
- 中文一律點陣字（倚天），字型檔玩家自備、不隨專案散布。
  **原版的畫面尺寸與字元格還沒確認**（EGA 640×350 與 MCGA 320×200 是兩套不同版面），
  在讀出原版版面之前不要定字級與畫布倍率——照 `~/.claude/rulebook/81-retro-cjk-hires-canvas.md`。
- 中英混排的排版格、標點禁則等決策等讀出原版版面之後再定。

### 3.3 這款遊戲的中文化難點（開工前先知道）

- **數字密度極高**：預算表、人口統計、評分表全是欄位對齊的數字。中文字寬是英數的兩倍，
  版面會爆。這不是翻譯問題，是版面問題，要當規格處理。
- **訊息是模板**：`_MSG.PTF` 裡的訊息很可能帶變數（城市名、年份、金額）。
  中文語序與英文不同，模板要能重排參數順序，不能寫死「前綴＋數字＋後綴」。
- **六套圖形集各有一份訊息檔**：`ASIA`／`MEDI`／`WEST`／`FUSA`／`FEUR`／`MOON`
  的建築名稱不同（古代亞洲的「寺廟」對上未來美國的某棟大樓）。翻譯目錄要照集分檔，
  不要合併成一份。

---

## 4. Remake 技術決策

Go / Ebiten，分層照地下城主那套（已驗證過在無頭環境可測）：

```
internal/assets   純解碼（PGF／PPF／PSN／PTF／PSF／CTY），回傳 image.RGBA 與結構，不認識 Ebiten
internal/sim      模擬規則層，不認識畫面；一張 128×128 陣列加上純量狀態
internal/ui       Ebiten 呈現層（textlayout／layout 刻意不依賴 Ebiten）
cmd/chengshi      進入點
tools/            go.sh（docker 建置）、對拍、抽取與整理腳本
```

- 建置與測試一律走 docker（`tools/go.sh`），不污染系統環境。
- **`internal/sim` 必須完全 headless、完全決定性。** 隨機數要自己實作並可指定種子，
  不准用 `math/rand` 的全域狀態。理由：§1.5 的逐 tick 對拍靠這個。
- **存檔策略是改寫不是重建**：從原始 bytes 出發只蓋已解欄位，未解區域一個 byte 都不動，
  驗收要做到讀出再寫回 byte-for-byte 相同。`.cty` 與 DOS 存檔可能是兩種格式，
  在確認之前不要假設相同。
- 測試存檔一律寫到 `/tmp` 或明確的測試輸出目錄，不覆蓋原版資料。
- 驗收要對原版行為，不是對自己的測試。測試全綠不等於玩得通；
  用 debug 捷徑跑完的流程要另外驗一次「正常玩家路徑」——對這款遊戲而言，
  正常玩家路徑是**從零開始蓋到一座能自我維持的城市**，以及**通關八個劇本**，
  不是「能放下一格住宅區」。

---

## 5. 工作紀律

- **完整性 > 投報。** 不准用「成本高、效益低」當跳過任何素材、格式、圖形集或劇本的理由。
  卡關就換方法並記錄，不要寫「暫緩／低投報」當結論。
- **文件要寫現況，不寫「當初怎麼錯的」。** 斷言被推翻就把正文改寫成正確答案，
  推翻紀錄集中到 `CONTEXT.md` 的「已被推翻的斷言」一處，正文最多留一個指標。
  那張表只放「已經知道錯、還不知道對」的；正確答案一旦定案就把那一列刪掉。
  教訓寫成規則，不要寫成事件敘述。
- **「唯一」「只有一處」沒有全檔掃描佐證就不要寫。**
- **讀任何函式之前先查 `docs/re/00-function-index.md`。**
- **要動某個機制之前，先在 `docs/re/00-wiring-status.md` 查它那一列。**
- **每一輪收尾**：更新 markdown → 清掉被推翻的斷言 → commit → 更新 `CONTEXT.md` 現況。
- 逐輪日期、命令、失敗、checkpoint 寫 `WORKLOG.md`，不要散回 README 與 CLAUDE.md。
- 未解的位元組要能原樣 round-trip。不要為了讓 worklist 看起來完整而發明遊戲規則。

`CONTEXT.md` 一旦建立就是全專案單一入口（現況、文件索引、術語表、oracle 優先序、
已被推翻的斷言、worklist）。對話被壓縮或換 session 接手時先讀它。

---

## 6. 環境硬規則

- 編譯一律走 docker；Python 一律 docker ＋ uv.venv，不動系統環境。
- 手打 `docker run` 一律帶 `--rm --log-opt max-size=10m --log-opt max-file=3`。
- **禁止任何 `docker image prune`／`system prune`／`volume prune`／`builder prune`／
  `rmi`／`container prune`。** 這台機器同時放著多個客戶專案的 image，誤刪過一次。
  只清理自己這次建立的容器。
- 派 subagent 時，上述邊界要寫進 prompt，不能只靠 agent 自律。

---

## 7. 授權

`LICENSE` 是專有授權：**非商業免費（含修改與再散布），商業使用要先洽談**
（`~/.claude/rulebook/85-retro-remake-licensing.md` 已定案，不重問）。

這個專案有一個前幾個專案沒有的授權張力，寫任何對外文字時都要照下面講：

- **本專案不是 Micropolis 的 fork，也不散布它的程式碼。** Micropolis 是 GPL-3.0；
  本專案把它當**可讀的規格書**，讀完寫成 `docs/spec/`，再用 Go 重寫。
  **一行都不照抄。** 這條不是為了規避授權，是 §0 的閘門要求：
  照抄會讓「這條規則從哪來、推論等級多少」永遠說不清楚。
- **這個立場有法律風險，而且要誠實揭露，不要粉飾。** 「讀 GPL 原始碼後重寫」
  是否構成衍生著作，實務上有爭議。`LICENSE` 的附註與 README 都要寫明：
  這是著作權人的立場，不是法院見解。
- **不要把本專案叫 open source。** 非商業限制不符合 OSI 的開源定義，
  對外一律寫 **source-available** 或「專有授權」。
- **不要把原版素材寫進授權範圍。** 條款第 1 條已經把界線劃掉：原版執行檔、資料檔、
  美術、音樂、點陣字型與掃描件不在內。
- **`LICENSE` 要出現在四個地方**：repo 根目錄、README 的授權段、**每一個發行包裡**、
  發行根目錄與 AppImage 的 `usr/share/doc/`。打包腳本要複製，不要靠人記得。
- SimCity 與 Maxis 是 Electronic Arts 的商標。repo 取名 `chengshi_cht` 就是為了避開；
  對外文字用商標只能作指示性使用，並附「與 EA 無隸屬關係」聲明。

---

## 8. 不做的事

- 不散布原版執行檔、資料檔、圖形集、音效、說明書掃描件、X11 發行包與倚天字型。
  發行包只有引擎程式碼與翻譯文本，玩家自備合法原版。
- 不照抄 Micropolis 的 C（授權允許，但違反 §0 的證據鏈要求，也違反 §7 的授權立場）。
- 不 vendor 任何參考素材進版控：`workplace/` 全部 gitignore。
- 不在還沒讀懂原版的情況下「先做個能跑的版本」。
- **不拿社群共識當規則來源**（§1.1 的警告）。
- 不做 SimCity 2000 以後的任何一代。這個專案只做 1989 年的初代。

---

## 9. 目錄結構

```
docs/re/          機制筆記（讀 Micropolis 的產物）
docs/re/00-master-index.md          總表（速查：函式、格式、結構、常數）
docs/re/00-function-index.md        函式索引（誰解過）
docs/re/00-wiring-status.md         接線狀態（結論 → remake 有沒有用上）
docs/re/00-knowledge-gaps.md        缺口檢查表
docs/formats/     資料格式規格（PGF／PPF／PSN／PTF／PSF／CTY／DOS 存檔）
docs/spec/        可實作規格（只有 READY 的能動工）
docs/manual-cht/  軟體世界說明書逐頁轉錄
docs/manual/      官方英文手冊 markdown ＋ 繁中
docs/research/    查證筆記（二手彙整，每條標來源；不是規格）
docs/walkthrough/ 繁中攻略
translations/     譯名表與語言資料（照圖形集分檔）
tools/            go.sh、對拍、抽取與整理腳本
workplace/        原版資料、Micropolis 參考原始碼、產生的中間檔（全部 gitignore）
internal/、cmd/   Go 引擎
```

---

## 10. 現在的狀態與下一步

現況與 worklist 的單一真相來源是 [`CONTEXT.md`](./CONTEXT.md)，動手前先讀它。

## 素材位置

- GitHub repo：<https://github.com/wicanr2/chengshi_cht>（private）
- DOS 版 1.10（破解版，見 §2.1 警告）：`./SimCity_DOS_EN_v110.zip`
- DUX X11 版發行包：`./Rare simcity.zip`
- 軟體世界珍藏版 29 中文說明書：`./珍029-模擬城市.rar`
- Micropolis 原始碼：<https://github.com/SimHacker/micropolis>（GPL-3.0，**尚未取得**）
- 骨灰集散地經典回顧：<https://boneash.oldgame.tw/temp/simcity/simcity.html>
- 方法論參考專案：`~/cht/dungeon_master/`、`~/cht/dragon/`
