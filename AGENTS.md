# 模擬城市繁體中文重製：協作規則

本檔是本儲存庫的 agent 入口。專案事實與細節仍以 `CONTEXT.md`、規格與證據文件為準；
歷史只看 `WORKLOG.md`，不要讓舊工作紀錄覆蓋目前狀態。

## 語言與工作環境

- 面向使用者、程式註解與新文件預設使用繁體中文；識別字、命令、檔名與產品名保留原文。
- 分析、搜尋大量資料、建置、測試、轉檔、抓圖、音訊與 GUI 自動化一律在 Docker 內執行。
- 一次性容器使用 `docker run --rm`、資源上限與預設 `--network none`；輸入唯讀掛載，
  可寫容器明確指定目前使用者 UID/GID。每批工作後檢查容器、映像與輸出擁有權。
- 不碰與任務無關的未追蹤檔或使用者修改；禁止用破壞性 Git 指令清理工作樹。

## 唯一現況與文件責任

- 開工先讀本檔、`CONTEXT.md`、`README.md`、`WORKLOG.md` 最新段落，以及本次功能直接相關的
  `docs/re/`、`docs/spec/`、`docs/formats/` 文件。
- `README.md` 只保存用途、穩定入口、執行方式、現況摘要與正式交付連結。
- `CONTEXT.md` 保存目前真相、有效決策、限制與最近交接；`WORKLOG.md` 只追加工作歷程。
- 可執行待辦若增加，應建立或更新唯一 `WORKLIST.md`，不可把流水帳堆進 README。
- 逆向研究寫入 `docs/re/` 或專門研究紀錄；格式與正式行為分別進 `docs/formats/`、
  `docs/spec/`，不要用未分級散文取代規格。

## 證據、規格與實作閘門

- 規則層的一手證據是固定版本的 Micropolis C 原始碼；DOS 1.10 資料檔是呈現、格式、
  劇本、訊息與音效的一手證據。DOS 執行檔反組譯只處理前兩者無法回答的 DOS 專屬行為。
- 證據等級固定為「已確認、強證據、假說、未解」。每項已確認結論保留輸入檔名與雜湊、
  工具版本、原始符號／位址／bytes 或可重播實驗，以及玩家體驗與交付影響。
- 正式行為遵循：`RE 證據 → DRAFT spec → 證據審查 → READY spec → implementation
  → 同狀態驗證 → CONFORMED`。沒有 READY 規格，不得修改正式規則或資料格式。
- 每份研究結論都要在 `docs/re/00-wiring-status.md` 登記並保持與程式一致；新證據推翻舊結論時，
  保留勘誤原因並同步修正現況、規格、接線與測試。
- 反組譯註記採非破壞性附加方式：永遠保留原始名稱、位址空間、線性位址、運算元與 bytes；
  語意、證據等級及來源另列。IDA `.i64` 的交叉參照與資料流優先於攤平 `.asm`。

## 完成與 polish 原則

- 開始任何工作前，把狀態分成「remake 已完成、原版 oracle 未知、可選 polish」。綠色單元測試
  只證明 remake 自洽，不能宣稱原版 parity。
- 優先修正常玩家會遇到的缺口。每個玩家功能至少驗一條「來源資料 → typed data → 規則 →
  UI／音訊 → 存檔 → 正常玩家路徑」垂直鏈；direct-entry 只能診斷，不能代替可玩證據。
- polish 不得暗改規則、存檔格式、資料 schema、原版忠實度或正式玩家路徑。遇到這類取捨，
  先查證事實並一次提出一個共同決策問題；在使用者確認前只做可撤回 prototype。
- 原版 DOS 版沒有背景音樂，已由 `docs/re/19-no-music.md` 確認。任何新增曲目都必須清楚標成
  remake 全新配樂，不得稱為原版遺失或還原音樂。
- 正式音樂、語音與音效以 Ogg Vorbis（`.ogg`）為主要播放與發行格式；WAV、PCM、MIDI 等
  僅作來源、研究 oracle 或明確相容性退路。保存來源、轉換參數、時長、雜湊、授權與聆聽紀錄。
- 新音樂需先確認風格、互動方式與散布權。正式資產至少驗可解碼、非靜音、峰值、循環接點、
  三輪疲勞、介面／音效可聽性與實際聆聽。

## 驗證、授權與交付

- 依風險執行 Go 測試、靜態檢查、翻譯／接線檢查、正常玩家路徑與同狀態原版對拍；
  環境／腳本失敗和產品缺陷分開記錄，修正後用同一容器與同一命令乾淨重跑。
- 專案程式碼、文件與譯文採根目錄 `LICENSE`；原版 EXE、資料、美術、說明書掃描與其他未授權
  素材不入 Git、不進公開發行包。音訊轉成 OGG 不會改變其權利狀態。
- 復古重製的唯一現行交付根目錄是 `dist-all/<版本>/`，其下分 `full/`、`release/`、`promo/`。
  含原版素材的 `full/` 只可留在合法的本機或私有位置；公開 `release/` 必須通過權利清單與
  deny-list。每包保存 manifest、SHA-256、建置版本、平台、啟動 smoke 與已知未驗項。
- 每輪交接更新 `WORKLOG.md`，記錄變更、驗證、未完成事項、工作樹與 Docker 清理狀態。
